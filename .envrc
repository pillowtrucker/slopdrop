# .envrc - For use with direnv
#
# This will automatically enter the development shell when you cd into this directory.
# Works with both flakes and non-flakes systems.
#
# Setup:
#   1. Install direnv: https://direnv.net/docs/installation.html
#   2. Hook direnv into your shell: https://direnv.net/docs/hook.html
#   3. Allow this directory: direnv allow

if command -v nix &> /dev/null; then
  if nix flake show &> /dev/null 2>&1; then
    # System supports flakes
    use flake
  else
    # Fallback for systems without flake support
    use nix
  fi
fi
